Brand marks are stored locally so the site does not depend on a third-party image host at runtime.
The original monochrome files use Simple Icons; marketplace marks follow the brands' current public identity.
